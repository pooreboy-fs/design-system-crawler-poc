{
  "name": "static-site-crawler",
  "version": "1.0.0",
  "description": "Crawls a React/JS site and outputs a deployable static HTML+CSS site",
  "main": "crawl.js",
  "scripts": {
    "crawl": "node crawl.js"
  },
  "dependencies": {
    "playwright": "^1.48.0",
    "fs-extra": "^11.2.0"
  }
}
